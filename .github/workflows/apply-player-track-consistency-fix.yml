name: Apply player track consistency fix

on:
  push:
    branches:
      - fix/player-track-consistency

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply player consistency fixes
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file_path = Path(path)
              text = file_path.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f'{path}: expected one match, found {count}')
              file_path.write_text(text.replace(old, new, 1), encoding='utf-8')

          player = 'src/renderer/store/player.ts'
          playlist = 'src/renderer/views/PlaylistPage.vue'
          tests = 'tests/liked-lyric-regression.spec.ts'

          replace_once(
              player,
              "    let lastUpdateTime = 0\n",
              "    let lastUpdateTime = 0\n    let trackLoadRevision = 0\n"
          )

          replace_once(
              player,
              "    const osdLyricStore = useOsdLyricStore()\n",
              r"""    const osdLyricStore = useOsdLyricStore()

    /**
     * 判断两个 Track 是否表示同一首正在播放的歌曲。
     *
     * 本地歌曲在在线匹配后 id 可能变化，因此优先使用 filePath 作为稳定身份；
     * 其它歌曲使用规范化后的 id 比较。
     */
    const sameTrackIdentity = (left: Track | null, right: Track) => {
      if (!left) return false
      if (left.filePath && right.filePath) return left.filePath === right.filePath
      return String(left.id) === String(right.id)
    }

    /**
     * 只有最新一次切歌事务才允许提交歌词、封面、MediaSession 和 OSD 状态。
     */
    const isCurrentTrackLoad = (revision: number, track: Track) =>
      revision === trackLoadRevision && sameTrackIdentity(currentTrack.value, track)
"""
          )

          replace_once(
              player,
              "    const currentIndex = ref(-1)\n",
              r"""    const currentIndex = ref(-1)

    /**
     * 切换歌曲时立即清除上一首歌曲派生出的显示状态，避免网络请求期间出现
     * “新音频 + 旧歌词/旧封面”的混合界面。
     */
    const clearTrackDerivedState = (track: Track) => {
      lyrics.value = []
      currentIndex.value = -1
      chorusStartTime.value = 0
      chorus.value = 0

      if (pic.value?.startsWith('blob:')) {
        URL.revokeObjectURL(pic.value)
      }
      pic.value = new URL('../assets/images/default.jpg', import.meta.url).href

      if (osdLyricStore.show) {
        const artists = track.artists ?? track.ar ?? []
        window.mainApi?.sendMessage({
          type: 'update-osd-status',
          data: {
            line: [-1, 0],
            seek: 0,
            title: `${artists[0]?.name || '未知歌手'} - ${track.name}`,
            pic: pic.value
          }
        })
      }
    }
"""
          )

          old_chorus = r"""    const loadCurrentTrackChorus = async (track: Track | null = currentTrack.value) => {
      chorusStartTime.value = 0
      chorus.value = 0

      if (!settingsStore.general.showChorus || !track?.matched) return

      const requestedTrackID = track.id
      try {
        const result = await songChorus(requestedTrackID)
        if (
          currentTrack.value?.id !== requestedTrackID ||
          !settingsStore.general.showChorus
        ) {
          return
        }

        const startTime = Number(result?.chorus?.[0]?.startTime)
        if (!Number.isFinite(startTime) || startTime < 0) return

        chorusStartTime.value = startTime / 1000
        updateChorusPosition()
      } catch (error) {
        console.warn('[Player] 获取副歌时间失败：', error)
      }
    }
"""
          new_chorus = r"""    const loadCurrentTrackChorus = async (
      track: Track | null = currentTrack.value,
      revision = trackLoadRevision
    ) => {
      if (!track || !isCurrentTrackLoad(revision, track)) return

      chorusStartTime.value = 0
      chorus.value = 0

      if (!settingsStore.general.showChorus || !track.matched) return

      const requestedTrackID = track.id
      try {
        const result = await songChorus(requestedTrackID)
        if (!isCurrentTrackLoad(revision, track) || !settingsStore.general.showChorus) {
          return
        }

        const startTime = Number(result?.chorus?.[0]?.startTime)
        if (!Number.isFinite(startTime) || startTime < 0) return

        chorusStartTime.value = startTime / 1000
        updateChorusPosition()
      } catch (error) {
        if (isCurrentTrackLoad(revision, track)) {
          console.warn('[Player] 获取副歌时间失败：', error)
        }
      }
    }
"""
          replace_once(player, old_chorus, new_chorus)

          old_match = r"""    const searchMatchForLocal = async (track: Track) => {
      if (track.type === 'local' && !track.matched) {
        const params = {
          title: track.name,
          album: '',
          artist: track.artists[0].name,
          duration: (track.dt || track.duration) / 1000,
          md5: track.md5,
          localID: track.id
        }
        const result = await searchMatch(params)
          .then((res: any) => {
            if (res.result.songs.length > 0) {
              const newTrack = res.result.songs[0]
              updateLocalID2OnlineID(track.id, newTrack.id)
              updateTrack(track.filePath, newTrack)
              return getALocalTrack({ filePath: track.filePath })
            }
          })
          .catch((err) => {
            showToast(err)
            return null
          })
        if (result) track = result
      }
      window.mainApi?.send('write-cover', {
        filePath: track.filePath,
        picUrl: track.matched ? track.album?.picUrl || track.al?.picUrl : null,
        currentPlayingPath: track.filePath
      })
      if (track.type === 'online' && !track.cache && settingsStore.autoCacheTrack.enable) {
        window.mainApi?.send('cacheATrack', { id: track.id, url: track.url })
      }
      await getCurrentTrackInfo(track)
      await updateMediaSessionMetaData(track)
      if (osdLyricStore.show) {
        window.mainApi?.sendMessage({
          type: 'update-osd-status',
          data: { title: `${(track.artists || track.ar)[0]?.name} - ${track.name}` }
        })
      }
    }
"""
          new_match = r"""    const searchMatchForLocal = async (track: Track, revision = trackLoadRevision) => {
      if (!isCurrentTrackLoad(revision, track)) return

      if (track.type === 'local' && !track.matched) {
        const params = {
          title: track.name,
          album: '',
          artist: track.artists[0].name,
          duration: (track.dt || track.duration) / 1000,
          md5: track.md5,
          localID: track.id
        }
        const result = await searchMatch(params)
          .then((res: any) => {
            if (!isCurrentTrackLoad(revision, track)) return null
            if (res.result.songs.length > 0) {
              const newTrack = res.result.songs[0]
              updateLocalID2OnlineID(track.id, newTrack.id)
              updateTrack(track.filePath, newTrack)
              return getALocalTrack({ filePath: track.filePath })
            }
          })
          .catch((err) => {
            if (isCurrentTrackLoad(revision, track)) showToast(err)
            return null
          })
        if (result) track = result
      }

      if (!isCurrentTrackLoad(revision, track)) return

      window.mainApi?.send('write-cover', {
        filePath: track.filePath,
        picUrl: track.matched ? track.album?.picUrl || track.al?.picUrl : null,
        currentPlayingPath: track.filePath
      })
      if (track.type === 'online' && !track.cache && settingsStore.autoCacheTrack.enable) {
        window.mainApi?.send('cacheATrack', { id: track.id, url: track.url })
      }

      const infoCommitted = await getCurrentTrackInfo(track, revision)
      if (!infoCommitted || !isCurrentTrackLoad(revision, track)) return

      const metadataCommitted = await updateMediaSessionMetaData(track, revision)
      if (!metadataCommitted || !isCurrentTrackLoad(revision, track)) return

      if (osdLyricStore.show) {
        window.mainApi?.sendMessage({
          type: 'update-osd-status',
          data: { title: `${(track.artists || track.ar)[0]?.name} - ${track.name}` }
        })
      }
    }
"""
          replace_once(player, old_match, new_match)

          old_info = r"""    const getCurrentTrackInfo = async (track: Track) => {
      if (!track) return
      void loadCurrentTrackChorus(track)
      await getLyric(track)
      currentIndex.value = getLyricIndex(lyrics.value, 0, 1)
    }

    const getLyric = async (track: Track) => {
      let data: lyricLine[] = []
      switch (track.type!) {
        case 'stream':
          data = await getStreamLyric(track)
          break
        case 'online':
          data = await getApiLyric(track.id)
          break
        case 'local':
          data = await _getLocalLyric(track)
          break
        default:
          break
      }

      data = data.filter((l) => !/^作(词|曲)\s*(:|：)\s*无$/.exec(l.lyric.text))
      if (data.length) {
        data.at(-1)!.end =
          data.at(-1)!.end || Math.min(currentTrackDuration.value, data.at(-1)!.start + 10)
      }
      const includeAM =
        data.length <= 10 && data.map((l) => l.lyric.text).includes('纯音乐，请欣赏')
      if (includeAM) {
        const reg = /^作(词|曲)\s*(:|：)\s*/
        const artists = currentTrack.value!.artists ?? currentTrack.value!.ar
        const author = artists[0]?.name
        data = data.filter((l) => {
          const regExpArr = l.lyric.text.match(reg)
          return !regExpArr || l.lyric.text.replace(regExpArr[0], '') !== author
        })
      }
      lyrics.value = data.length === 1 && includeAM ? [] : data
    }
"""
          new_info = r"""    const getCurrentTrackInfo = async (track: Track, revision = trackLoadRevision) => {
      if (!track || !isCurrentTrackLoad(revision, track)) return false

      void loadCurrentTrackChorus(track, revision)
      const committed = await getLyric(track, revision)
      if (!committed || !isCurrentTrackLoad(revision, track)) return false

      currentIndex.value = getLyricIndex(lyrics.value, 0, 1)
      return true
    }

    const getLyric = async (track: Track, revision = trackLoadRevision) => {
      let data: lyricLine[] = []
      switch (track.type!) {
        case 'stream':
          data = await getStreamLyric(track)
          break
        case 'online':
          data = await getApiLyric(track.id)
          break
        case 'local':
          data = await _getLocalLyric(track)
          break
        default:
          break
      }

      if (!isCurrentTrackLoad(revision, track)) return false

      data = data.filter((l) => !/^作(词|曲)\s*(:|：)\s*无$/.exec(l.lyric.text))
      const trackDuration = ~~((track.dt || track.duration || 1000) / 1000)
      if (data.length) {
        data.at(-1)!.end =
          data.at(-1)!.end || Math.min(trackDuration, data.at(-1)!.start + 10)
      }
      const includeAM =
        data.length <= 10 && data.map((l) => l.lyric.text).includes('纯音乐，请欣赏')
      if (includeAM) {
        const reg = /^作(词|曲)\s*(:|：)\s*/
        const artists = track.artists ?? track.ar ?? []
        const author = artists[0]?.name
        data = data.filter((l) => {
          const regExpArr = l.lyric.text.match(reg)
          return !regExpArr || l.lyric.text.replace(regExpArr[0], '') !== author
        })
      }

      if (!isCurrentTrackLoad(revision, track)) return false
      lyrics.value = data.length === 1 && includeAM ? [] : data
      return true
    }
"""
          replace_once(player, old_info, new_info)

          old_replace = r"""    const replaceCurrentTrack = async (trackID: number | string, autoPlay = true) => {
      cancelSleepTimerForTrackChange(trackID)
      if (autoPlay && currentTrack.value?.name) {
        scrobbleFM(currentTrack.value, seek.value)
      }

      return getLocalMusic(trackID as number).then(async (track) => {
        if (!track) {
          nextTrackCallback()
          return false
        }
        const resumePosition =
          !autoPlay &&
          currentTrack.value?.id !== undefined &&
          String(currentTrack.value.id) === String(trackID)
            ? seek.value
            : 0
        currentTrack.value = track
        seek.value = resumePosition
        searchMatchForLocal(track!)
        const source = await getTrackSource(track!)
        let replaced = false
        if (source) {
          if (track!.id === currentTrack.value?.id) {
            playAudioSource(source, autoPlay)
            replaced = true
          }
        } else {
          showToast(track?.reason)
          _playNextTrack(isPersonalFM.value)
        }
        if (autoPlay && currentTrack.value?.type === 'stream') {
          scrobbleStream(track)
        }
        return replaced
      })
    }
"""
          new_replace = r"""    const replaceCurrentTrack = async (trackID: number | string, autoPlay = true) => {
      const revision = ++trackLoadRevision
      cancelSleepTimerForTrackChange(trackID)
      if (autoPlay && currentTrack.value?.name) {
        scrobbleFM(currentTrack.value, seek.value)
      }

      return getLocalMusic(trackID as number).then(async (track) => {
        if (revision !== trackLoadRevision) return false
        if (!track) {
          nextTrackCallback()
          return false
        }

        const resumePosition =
          !autoPlay &&
          currentTrack.value?.id !== undefined &&
          String(currentTrack.value.id) === String(trackID)
            ? seek.value
            : 0

        currentTrack.value = track
        clearTrackDerivedState(track)
        seek.value = resumePosition
        void searchMatchForLocal(track, revision)

        const source = await getTrackSource(track)
        if (!isCurrentTrackLoad(revision, track)) return false

        if (!source) {
          showToast(track?.reason)
          void _playNextTrack(isPersonalFM.value)
          return false
        }

        const replaced = await playAudioSource(source, autoPlay, revision, track)
        if (!replaced || !isCurrentTrackLoad(revision, track)) return false

        if (autoPlay && track.type === 'stream') {
          scrobbleStream(track)
        }
        return true
      })
    }
"""
          replace_once(player, old_replace, new_replace)

          old_audio = r"""    const playAudioSource = async (source: string, autoPlay = true) => {
      // 切歌时先淡出
      const fade = fadeDuration.value
      await smoothGain(0, fade)
      audioNodes.audio!.removeAttribute('src')
      audioNodes.audio!.load()
      audioNodes.audio!.src = source
      audioNodes.audio!.load()
      if (autoPlay) {
        playing.value = await play()
      }
    }
"""
          new_audio = r"""    const playAudioSource = async (
      source: string,
      autoPlay = true,
      revision = trackLoadRevision,
      track: Track | null = currentTrack.value
    ) => {
      if (!track || !isCurrentTrackLoad(revision, track)) return false

      // 切歌时先淡出；淡出结束后再次确认事务仍然是最新切歌请求。
      const fade = fadeDuration.value
      await smoothGain(0, fade)
      if (!isCurrentTrackLoad(revision, track)) return false

      audioNodes.audio!.removeAttribute('src')
      audioNodes.audio!.load()
      audioNodes.audio!.src = source
      audioNodes.audio!.load()
      if (autoPlay) {
        playing.value = await play()
      }
      return isCurrentTrackLoad(revision, track)
    }
"""
          replace_once(player, old_audio, new_audio)

          old_metadata = r"""    const updateMediaSessionMetaData = async (track: Track) => {
      if ('mediaSession' in navigator === false) return

      if (pic.value?.startsWith('blob:')) {
        URL.revokeObjectURL(pic.value)
      }
      pic.value = await getPic(track, 512)

      const arts = track.artists ?? track.ar
      const artists = arts.map((a) => a.name)
      const metadata = {
        title: track.name,
        artist: artists.join(','),
        album: track.album?.name ?? track.al?.name,
        artwork: [
          {
            src: await getPic(track, 512),
            type: 'image/jpg',
            sizes: '512x512'
          },
          {
            src: await getPic(track, 1024),
            type: 'image/jpg',
            sizes: '1024x1024'
          }
        ],
        length: currentTrackDuration.value,
        trackId: track.id,
        url: '/trackid/' + track.id,
        progress: audioNodes.audio?.currentTime ?? 0,
        rate: playbackRate.value,
        asText: lyrics.value.map((lrc) => `${formatTime(lrc.start)}${lrc.lyric.text}`).join('\n'),
        lyricOffset: lyricOffset.value
      }
      if (window.env?.isWindows) {
        metadata.artwork = [
          {
            src: await getPic(track, 2048),
            type: 'image/jpg',
            sizes: '2048x2048'
          }
        ]
      }
      navigator.mediaSession.metadata = null
      navigator.mediaSession.metadata = new MediaMetadata(metadata)
      if (window.env?.isLinux) {
        if (track.type === 'stream') {
          metadata.artwork.map((art) => {
            const url = `http://localhost:${window.env?.isDev ? 40001 : 41830}` + art.src
            art.src = url
          })
        } else if (track.type === 'local') {
          metadata.artwork.map((art) => {
            const url = `http://localhost:${window.env?.isDev ? 40001 : 41830}/local-asset?id=${track.id}&size=${art.sizes.split('x')[0]}`
            art.src = url
          })
        }
        window.mainApi?.send('metadata', metadata)
      }
    }
"""
          new_metadata = r"""    const updateMediaSessionMetaData = async (
      track: Track,
      revision = trackLoadRevision
    ) => {
      if (!isCurrentTrackLoad(revision, track)) return false

      const nextPic = await getPic(track, 512)
      if (!isCurrentTrackLoad(revision, track)) {
        if (nextPic?.startsWith('blob:')) URL.revokeObjectURL(nextPic)
        return false
      }

      if ('mediaSession' in navigator === false) {
        if (pic.value?.startsWith('blob:') && pic.value !== nextPic) {
          URL.revokeObjectURL(pic.value)
        }
        pic.value = nextPic
        return true
      }

      let artwork: { src: string; type: string; sizes: string }[]
      if (window.env?.isWindows) {
        const pic2048 = await getPic(track, 2048)
        if (!isCurrentTrackLoad(revision, track)) {
          if (nextPic?.startsWith('blob:')) URL.revokeObjectURL(nextPic)
          if (pic2048?.startsWith('blob:')) URL.revokeObjectURL(pic2048)
          return false
        }
        artwork = [{ src: pic2048, type: 'image/jpg', sizes: '2048x2048' }]
      } else {
        const pic1024 = await getPic(track, 1024)
        if (!isCurrentTrackLoad(revision, track)) {
          if (nextPic?.startsWith('blob:')) URL.revokeObjectURL(nextPic)
          if (pic1024?.startsWith('blob:')) URL.revokeObjectURL(pic1024)
          return false
        }
        artwork = [
          { src: nextPic, type: 'image/jpg', sizes: '512x512' },
          { src: pic1024, type: 'image/jpg', sizes: '1024x1024' }
        ]
      }

      if (!isCurrentTrackLoad(revision, track)) return false

      if (pic.value?.startsWith('blob:') && pic.value !== nextPic) {
        URL.revokeObjectURL(pic.value)
      }
      pic.value = nextPic

      const arts = track.artists ?? track.ar ?? []
      const artists = arts.map((a) => a.name)
      const trackDuration = ~~((track.dt || track.duration || 1000) / 1000)
      const metadata = {
        title: track.name,
        artist: artists.join(','),
        album: track.album?.name ?? track.al?.name,
        artwork,
        length: trackDuration,
        trackId: track.id,
        url: '/trackid/' + track.id,
        progress: audioNodes.audio?.currentTime ?? 0,
        rate: playbackRate.value,
        asText: lyrics.value.map((lrc) => `${formatTime(lrc.start)}${lrc.lyric.text}`).join('\n'),
        lyricOffset: lyricOffset.value
      }

      if (!isCurrentTrackLoad(revision, track)) return false
      navigator.mediaSession.metadata = null
      navigator.mediaSession.metadata = new MediaMetadata(metadata)

      if (osdLyricStore.show) {
        window.mainApi?.sendMessage({
          type: 'update-osd-status',
          data: { pic: pic.value }
        })
      }

      if (window.env?.isLinux) {
        if (track.type === 'stream') {
          metadata.artwork.map((art) => {
            const url = `http://localhost:${window.env?.isDev ? 40001 : 41830}` + art.src
            art.src = url
          })
        } else if (track.type === 'local') {
          metadata.artwork.map((art) => {
            const url = `http://localhost:${window.env?.isDev ? 40001 : 41830}/local-asset?id=${track.id}&size=${art.sizes.split('x')[0]}`
            art.src = url
          })
        }
        if (isCurrentTrackLoad(revision, track)) {
          window.mainApi?.send('metadata', metadata)
        }
      }
      return true
    }
"""
          replace_once(player, old_metadata, new_metadata)

          replace_once(
              player,
              "    const resetPlayer = (resetBiq = true) => {\n",
              "    const resetPlayer = (resetBiq = true) => {\n      trackLoadRevision += 1\n"
          )

          replace_once(
              playlist,
              "const { replacePlaylist } = playerStore\n",
              "const { replacePlaylist } = playerStore\n\nlet intelligenceRequestRevision = 0\n"
          )

          old_intelligence = r"""const playIntelligenceList = () => {
  const randomId = Math.floor(Math.random() * tracks.value.length + 1)
  const songId = tracks.value[randomId].id
  intelligencePlaylist({ id: songId, pid: likedSongPlaylistID.value }).then((result) => {
    const trackIDs = result.data.map((t: any) => t.id)
    const idx = _shuffle.value ? Math.floor(Math.random() * trackIDs.length) : 0
    replacePlaylist('playlist', likedSongPlaylistID.value, trackIDs, idx)
  })
}
"""
          new_intelligence = r"""const playIntelligenceList = async () => {
  if (!tracks.value.length || !likedSongPlaylistID.value) {
    showToast('心动模式暂无可用歌曲')
    return
  }

  const requestRevision = ++intelligenceRequestRevision
  const randomIndex = Math.floor(Math.random() * tracks.value.length)
  const songId = tracks.value[randomIndex]?.id
  if (!songId) {
    showToast('心动模式暂无可用歌曲')
    return
  }

  try {
    const result = await intelligencePlaylist({ id: songId, pid: likedSongPlaylistID.value })
    if (requestRevision !== intelligenceRequestRevision) return

    const trackIDs = (Array.isArray(result?.data) ? result.data : [])
      .map((track: any) => Number(track?.id))
      .filter((id: number) => Number.isFinite(id))

    if (!trackIDs.length) {
      showToast('心动模式加载失败，请稍后重试')
      return
    }

    const idx = _shuffle.value ? Math.floor(Math.random() * trackIDs.length) : 0
    await replacePlaylist('playlist', likedSongPlaylistID.value, trackIDs, idx)
  } catch (error) {
    if (requestRevision !== intelligenceRequestRevision) return
    console.warn('[Playlist] 心动模式加载失败：', error)
    showToast('心动模式加载失败，请稍后重试')
  }
}
"""
          replace_once(playlist, old_intelligence, new_intelligence)

          test_block = r"""

test.describe('player track identity consistency', () => {
  test('uses a monotonic revision to reject stale asynchronous track work', () => {
    const player = readSource('src/renderer/store/player.ts')

    expect(player).toContain('let trackLoadRevision = 0')
    expect(player).toContain('const isCurrentTrackLoad =')
    expect(player).toContain('const revision = ++trackLoadRevision')
    expect(player).toContain('clearTrackDerivedState(track)')
    expect(player).toContain('void searchMatchForLocal(track, revision)')
    expect(player).toContain('await playAudioSource(source, autoPlay, revision, track)')
  })

  test('commits lyrics, artwork and media metadata only for the active track load', () => {
    const player = readSource('src/renderer/store/player.ts')

    expect(player).toContain('const getLyric = async (track: Track, revision = trackLoadRevision) =>')
    expect(player).toContain('if (!isCurrentTrackLoad(revision, track)) return false')
    expect(player).toContain('const updateMediaSessionMetaData = async (')
    expect(player).toContain('const trackDuration = ~~((track.dt || track.duration || 1000) / 1000)')
    expect(player).toContain("data: { pic: pic.value }")
  })

  test('keeps heart-mode random indexing in bounds and ignores stale requests', () => {
    const playlist = readSource('src/renderer/views/PlaylistPage.vue')

    expect(playlist).toContain('let intelligenceRequestRevision = 0')
    expect(playlist).toContain('Math.floor(Math.random() * tracks.value.length)')
    expect(playlist).not.toContain('Math.floor(Math.random() * tracks.value.length + 1)')
    expect(playlist).toContain('requestRevision !== intelligenceRequestRevision')
    expect(playlist).toContain('Array.isArray(result?.data)')
    expect(playlist).toContain("showToast('心动模式加载失败，请稍后重试')")
  })
})
"""
          test_path = Path(tests)
          test_text = test_path.read_text(encoding='utf-8')
          if "test.describe('player track identity consistency'" in test_text:
              raise RuntimeError('regression test block already exists')
          test_path.write_text(test_text.rstrip() + test_block + '\n', encoding='utf-8')
          PY

      - name: Validate source patch
        shell: bash
        run: |
          git diff --check
          python3 <<'PY'
          from pathlib import Path
          player = Path('src/renderer/store/player.ts').read_text(encoding='utf-8')
          playlist = Path('src/renderer/views/PlaylistPage.vue').read_text(encoding='utf-8')
          tests = Path('tests/liked-lyric-regression.spec.ts').read_text(encoding='utf-8')
          required = [
              'let trackLoadRevision = 0',
              'const isCurrentTrackLoad =',
              'const revision = ++trackLoadRevision',
              'await playAudioSource(source, autoPlay, revision, track)',
              'let intelligenceRequestRevision = 0',
              'requestRevision !== intelligenceRequestRevision',
              "test.describe('player track identity consistency'"
          ]
          combined = player + playlist + tests
          missing = [item for item in required if item not in combined]
          if missing:
              raise SystemExit(f'missing expected patch markers: {missing}')
          if 'Math.floor(Math.random() * tracks.value.length + 1)' in playlist:
              raise SystemExit('heart-mode out-of-bounds index still present')
          PY

      - name: Commit source fix and remove staging workflow
        shell: bash
        run: |
          rm .github/workflows/apply-player-track-consistency-fix.yml
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/renderer/store/player.ts src/renderer/views/PlaylistPage.vue tests/liked-lyric-regression.spec.ts .github/workflows/apply-player-track-consistency-fix.yml
          git commit -m "fix(player): keep track metadata synchronized"
          git push origin "HEAD:${GITHUB_REF_NAME}"

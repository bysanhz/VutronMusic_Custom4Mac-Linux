name: Track consistency patch runner

on:
  push:
    branches:
      - "fix/player-track-consistency"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "workflow runner is available"

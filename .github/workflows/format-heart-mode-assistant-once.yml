name: One-time format Heart Mode assistant

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/format-heart-mode-assistant-once.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile --ignore-scripts --network-timeout 600000

      - name: Format changed component
        run: yarn prettier src/renderer/components/HeartModeAssistant.vue --write

      - name: Commit formatting and cleanup
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/renderer/components/HeartModeAssistant.vue
          git rm .github/workflows/format-heart-mode-assistant-once.yml
          git commit -m "chore: format Heart Mode assistant"
          git push origin HEAD:main
